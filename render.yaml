services:
  - type: web
    name: easy-update-server
    env: node
    plan: starter
    rootDir: .
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter @easy-update/server build
    startCommand: cd /opt/render/project/src && node apps/server/dist/index.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: MANAGED_AI_API_KEY
        sync: false
      - key: CLIENT-WEB_URL
        sync: false
      - key: NODE_VERSION
        value: 22